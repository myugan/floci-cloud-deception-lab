apiVersion: v1
kind: Service
metadata:
  name: ray-dashboard
  namespace: floci-deception
spec:
  selector:
    app: floci-deception
  ports:
    - port: 8265
      targetPort: 8265
      name: dashboard
